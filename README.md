# Barani-resume

Resume generator (Node.js) — generates a polished developer resume PDF from data stored in MongoDB.

## Features

- Store/update resume data in MongoDB
- Generate a well-formatted PDF using Puppeteer
- Clickable email / phone / GitHub / LinkedIn links
- Easy to edit `finalResume` object for quick changes
- GitHub Actions workflow (optional) to auto-generate `resume.pdf` on push

## Quick start (local)

1. Clone repo:
```bash
git clone https://github.com/barani123-tharan/Barani-resume.git
cd Barani-resume
